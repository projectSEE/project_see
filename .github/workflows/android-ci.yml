name: Flutter Android CI/CD

on:
  workflow_dispatch:  # Allow manual triggering
  push:
    branches: [ "development" ]
  pull_request:
    branches: [ "development" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Get the code
      - name: Checkout Code
        uses: actions/checkout@v4

      # 2. Set up Java (Required for Android)
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3. Set up Flutter
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      # 4. Get Flutter dependencies
      - name: Get dependencies
        run: flutter pub get

      # 5. Analyze code (only fail on errors, not infos/warnings)
      - name: Analyze code
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      # 6. Run Tests (skipped - tests need Firebase mock setup)
      # - name: Run Tests
      #   run: flutter test

      # 7. Build the APK
      - name: Build Release APK
        run: flutter build apk --release

      # 8. Upload APK as artifact (for download)
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: build/app/outputs/flutter-apk/app-release.apk

      # 9. Upload to Firebase (Only runs on push to development or manual trigger)
      - name: Upload to Firebase App Distribution
        if: github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/development')
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_APP_ID }}
          serviceCredentialsFileContent: ${{ secrets.FIREBASE_SERVICE_CREDENTIALS }}
          groups: beta_testers
          file: build/app/outputs/flutter-apk/app-release.apk
